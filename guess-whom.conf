{
    host => 'mojolicious-dev:3000',
    ws_host => 'mojolicious-dev:3000',
    passphrase => 'DEFAULT',
    db => 'guess-whom.db',
}

# After configuring, don't forget to create the public/face/ directory and
# make it writable by the guess-whom user
